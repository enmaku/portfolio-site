/**
 * Default dependency bag for dashboard tile loaders on the stats page.
 * @returns {import('./tiles/totalMatchesLoader.js').TotalMatchesLoaderDeps}
 */
export function createDungeonRunnerStatsTileDeps() {
  return {}
}
