/**
 * Shared trade-clearing constants (leaf — no clearing imports).
 * Domain: world-builder/CONTEXT.md — port toll, faction tax.
 */

/** Baseline port toll: 5% of full local price. */
export const PORT_TOLL_RATE = 0.05

/** Baseline faction tax: 10% of prior-epoch realized on-map income. */
export const FACTION_TAX_RATE = 0.1
